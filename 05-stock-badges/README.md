# 05 - Stock Badges

Displays a small badge for low-stock or sold-out products based on variant inventory.

Usage
- Copy `snippet.liquid` into your product template near the add-to-cart area.

Customization
- `STOCK_THRESHOLD` in the top comment of `snippet.liquid`: ``

Notes
- If `inventory_management` is `null` (untracked), the snippet intentionally shows nothing.

Demo
- demo.gif placeholder pending — please record and add `demo.gif` in this folder.
